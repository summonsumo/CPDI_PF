local snapping = {}
    function snapping.new(xsnap,ysnap,x,y,code)

        local snappoint = display.newRect(xsnap,ysnap,x,y)
        local snapobject = display.newRect(xsnap + math.random(xsnap, xsnap + 50), ysnap + math.random(0,50),x,y)



        
        

    end
return snapping